Sources for the plugin `plugin.appodeal.beta.Yandex`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.Yandex" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
